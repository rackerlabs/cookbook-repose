<?xml version="1.0" encoding="UTF-8"?>

<!-- http://wiki.openrepose.org/display/REPOSE/IP+User+filter -->
<ip-user xmlns="http://docs.openrepose.org/repose/ip-user/v1.0">
    <user-header name="<%= @user_header_name %>" quality="<%= @user_header_quality %>"/>
    <group-header name="<%= @group_header_name" quality="<%= @group_header_quality %>"/>
TODO - needs a loop for groups/cidr-ip here
    for each item in group put group and any cidr-ip values
    <group name=<%= @group_name %>>
        <cidr-ip><%= @cidr-ip %></cidr-ip>
    </group>
</ip-user>
